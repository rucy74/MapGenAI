# MapGen AI 텍스트 우선 개발 계획

2026-09-13. 사용자 결정: “이미지쪽은 나중에”, “텍스트 요청 먼저 전부 다 되게 만드는 걸 1차 목표”. 이후 “지금부터 fable 호출하지 말아줘. 된다고 할때까지. 크레딧이 없어서”로 **Fable 자문은 재허용 전까지 중단**했다. 최신 요청 “나중에 다시 다루기 전까지 이미지 넣어서 하는 기반 기능 꺼놔줘”에 따라 이미지 UI·적용·생성 효과를 일시 중단하고 코드·데이터만 보존한다.

## 결론과 현재 상태

전체 재작성 대신 기존 도형 계산, 고유 ID 편집, 누적 변경, 타일별 상태, Undo·프리셋을 재사용했다. **도형의 영역 → 지형 재료 → 구조물 배치**의 공통 경로를 추가했고, 2026-09-14에는 지형과의 공간 관계와 게임 기본 고대 위협 생성기를 연결했다.

| 요청 | 현재 동작 | 다음 구현 |
|---|---|---|
| 원형/자연스러운 원형 | 정확형 기본, 요청 시 경계 굴곡 지원 | 텍스트 연속 편집 회귀 유지 |
| 삼각주·해안 변형 편집 | 월드 연결을 유지하며 일반 강·해안으로 복귀 | 다른 특징의 실제 생성 검사 확대 |
| 특징 추가 조건 / 내부 특징 제거·복원 | 실제 Odyssey·VLE 정의와 생성기 조건으로 공통 검사 | 다른 모드 조합은 별도 실생성 검증 |
| 특정 영역 용암 채움 | 활성 영구 TerrainDef와 LavaDeep/CooledLava/VolcanicRock 지원 | 추가 재료·모드 조합의 실제 생성 검증 |
| 섬 안/강가/산기슭에 유적 배치 | 전체 면적·실제 지형 거리/방향·최소 간격·90도 회전, 손상된 벽/바닥 폐허 | 다른 구조물 생성기·관계 종류 확대 |
| 특정 위치의 고대 위협 | 게임 기본 생성기, 15~20칸, 전체최대4개, 난이도/DLC 내용물, 주황색 미리보기 예약 | 내부 회전·특정 적/전리품 지정은 별도 작업 |
| 임의 모드 지형/구조물 | 재료는 영구 지형 조건 검사, 구조물은 ruin/ancient_danger 지원 | 모드·퀘스트별 검증된 생성기 연결 |
| 이미지 입력 기반 기능 | UI·적용·생성 효과 비활성, 저장 데이터 보존 | 사용자가 다시 다루기로 할 때 재검토 |

“모든 텍스트 요청”은 지원 항목을 체계적으로 늘려가는 방향이다. 설치되지 않은 콘텐츠나 임의의 게임 동작을 무제한 생성한다는 보장은 아니다. 지원하지 않는 요청은 무엇이 부족한지 설명하고, 다른 결과로 몰래 대체하지 않는다.

## 1. 기존 특징을 유지·제거·복원

이번 구현은 `actual_tile_features`로 원래부터 있던 특징까지 전달한다. `remove_mutators`는 개별 특징, `remove_categories`는 종류 전체의 생성 제외, `restore_categories`는 그 제외 해제다. 잘못된 이름과 모순된 요청은 전체 변경을 거부한다. 기존 저장의 비활성 모드 제거 기록은 다른 편집을 막지 않는다.

사용자는 이후 “내륙에서 해안같은 건 만들면 안된다고 생각하는데”, “삼각주같은 건 강이 있을 때만 가능한거고 등등. 그런 식으로”라고 지정했다. 근거: `agents/main/log/2026-09-13-1357-mapgenai.md` §2026-09-13 — 월드 연결 보존 정책 구현 승인 [decision] (프로젝트 루트 기록).

이에 따라 이전의 강·해안 전체 생성 제외 방식은 **월드 연결 보존 정책으로 대체**한다. 삼각주·피오르드 등 변형만 제거하면 일반 강·해안이 남는다. 강 방향과 기존 해안 방향은 편집할 수 있고, 내부 호수·오아시스 등은 실제 지리·바이옴 조건이 맞으면 추가한다.

`FeaturePolicy`는 상속·외부 XML 패치 이후의 실제 정의와 생성기 검사를 사용하며 프롬프트 후보와 backend 계획·프리셋에서 같은 환경 조건을 적용한다. 자연 출현 확률, 기존 특징과의 자기 중복 판정, 랜드마크 추첨 제한은 환경 조건과 분리한다. 실제 특징 목록을 수정하되 월드 랜드마크 이름·아이콘을 재배치하지 않는다. 기존 자연 특징은 현재 유지 중인 경우 보존하고, 제거 후 재추가·복원에는 현행 조건을 검사한다.

종류 제거는 해당 TileMutator를 제외하는 처리다. 바이옴·기본 생성기·별도 외부 모드가 같은 효과를 다시 만드는 경우까지 모두 제거한다고 주장하지 않는다. [조건 정책 구현·검증](analysis/2026-09-13-feature-policy/report.md)에 구체적 범위를 기록한다.

## 2. 영역을 재사용할 수 있게 분리

현재 composite SDF가 계산한 영역을 채움뿐 아니라 구조물 배치에도 사용한다. 새 그림/팔레트 엔진은 만들지 않는다. 영역은 안정적인 ID로 저장하고, 합집합·차집합·섬 구멍·자연스러운 경계를 그대로 재사용한다.

예를 들어 “용암 호수 가운데 섬”은 용암 영역과 섬 영역의 차집합이며, “그 섬에 유적”은 같은 섬 ID를 참조한다. 유적이 섬의 바깥 사각형이나 용암 위에 놓이지 않도록 **전체 건물 면적이 실제 영역 안에 들어가는지** 검사한다.

## 3. 지형 채움 재료 목록

기존 비옥도 값 인코딩은 하위 호환용으로 남기고, 최종 채움은 영역별 재료 이름을 활성 TerrainDef로 해석해 적용한다. 기존 `water`, `soil` 등 별칭도 보존한다. 생성 단계400에서 앞선 지형 특징 처리를 반영한 뒤 적용하며 강·바다·도로는 보호한다.

- 첫 대상: 기존 7종과 Odyssey 용암. 게임에 `LavaDeep`, `LavaShallow`, `CooledLava`가 존재하는 것을 로컬 정의에서 확인했다.
- 얕은 용암은 임시 지형이며 제거 후 CooledLava로 전환되므로 현재 fill에서 제외했다. 깊은 용암은 게임 자체의 통행 불가·열·화상 속성을 사용한다.
- DLC/제공 모드가 없으면 요청 단계에서 안내한다. 모르는 재료를 물로 바꾸지 않는다.
- 채움 결과가 다른 생성 단계에 덮어써지는지, 통행·강 흐름·다리와 충돌하는지 실제 맵으로 확인한다.

완료 기준: 용암 원/고리/자연스러운 경계, 가운데 섬 보존, 재료만 바꾸기, Undo·프리셋·Scribe, DLC 없는 경우의 오류, 기존 물/토양 결과 보존.

## 4. 구조물 배치 계획과 생성기

현재 구조물 계획은 ID, 종류, 개수, 대상 영역/좌표/경계, 크기/seed, relation/rotation/spacing을 저장한다. “유적 밀도”와 “특정 섬 위 유적”은 별도 기능이다. 실제 강/물/산/영역 안쪽 경계까지의 거리, 동서남북 방향, 90도 회전과 최소 빈칸 간격을 구현했다. 임의 좌표 추측으로 관계를 대체하지 않으며, 정확히 같은 간격·곡선 방향 자동 회전은 후속이다.

손상된 화강암 벽·바닥의 단순 폐허와 게임 기본 ancientTemple 생성기를 연결한 고대 위협을 구현했다. 고대 위협은 지붕·내부 내용물·전리품·경고와 평화로운 난이도 규칙을 사용하며 BaseGen 공유 객체를 복원한다. 공간 제약은 두 생성기가 공유한다. 외부 모드용 공개 등록 API는 아직 없으며 주둔지·퀘스트 구조물은 전리품·세력·위험·퀘스트 상태를 추가로 검증해야 한다.

공간이 부족하면 요청한 위치 지정 구조물 batch를 미생성하고 실패를 표시한다. 축소나 영역 밖 이동은 자동으로 하지 않는다. Map Preview는 폐허 벽 계획과 고대 위협의 주황색 예약 테두리를 표시하며, 고대 위협의 실제 내부는 생성하지 않는다. 실제 맵은 단계800에서 기존 건물·예약 공간을 포함해 다시 검사한다. 따라서 미리보기 배치가 실제 맵의 동일 위치를 보장하지는 않는다. 네이티브 생성기 내부의 예상하지 못한 중간 실패까지 전체 물체 rollback을 보장하는 것은 아니다.

현재 검증 대상: 섬 안/좌표·경계/여러 개, 전체 면적, 공간 부족, 순차 편집/Undo, 이미지 중단, 활성 재료 오류, 강 양쪽/산기슭/호숫가/섬 가장자리·최소 간격·폐허 회전, 실제 고대 위협 내용물과 난이도. [기본 구현](analysis/2026-09-13-text-regions/report.md), [연속 복합 요청](analysis/2026-09-14-text-expansion/compound-report.md), [공간 관계](analysis/2026-09-14-text-expansion/spatial-report.md), [고대 위협](analysis/2026-09-14-text-expansion/ancient-report.md)를 참조한다.

## 5. 요청 해석과 실행 결과를 함께 평가

지원 기능 목록을 프롬프트와 검증기에서 공유한다. AI가 그럴듯한 문장을 답한 것만으로 성공 처리하지 않는다. 요청 → 계획 → 실제 TerrainDef/건물 위치를 비교하고, 미지원·부분 적용·완료를 구분한다.

실제 댓글 요청, 이번 자연어 요청, 기존 지형 보존을 묶은 반복 편집 사례를 회귀 목록으로 유지한다. Gemini 3.8을 기준으로 먼저 검증하고, 사용자가 선택할 수 있는 하위 모델은 별도 결과로 기록한다. Fable 재허용 전까지 추가 Claude 자문은 호출하지 않는다.

근거: [이번 수정과 검증](analysis/2026-09-13-text-first/report.md), [기존 댓글·실패 기록](analysis/2026-09-13-review/evidence.md), [사용자 설명서](description-ko.md).
