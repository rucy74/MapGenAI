# Claude consultation

Requested: claude-fable-5-1

Observed modelUsage: ["claude-haiku-4-5-20251001", "claude-fable-5-1"]

## 검토 결과: 차단할 코드 결함 없음

결론(3.8 기본화, Simple에서 3.8/2.5 명시 저장, Advanced 선택 유지, 무료티어 문구 제거)은 제시된 근거와 부합합니다. 범례 효과와 모델 효과를 분리했고 임의 사진으로 일반화하지 않은 점도 적절합니다.

### 코드에서 확인한 사항 (비차단)

- **HTTP 오류 본문 폐기**: 실패 시 Google의 실제 오류 메시지(예: `thinkingLevel` 미지원, 모델 404, 지역 제한)를 버리고 상태코드만 던집니다. 3.8이 특정 키·지역에서 안 될 때 사용자가 원인을 알 수 없으므로, 본문의 `error.message`를 짧게 붙이는 편이 좋습니다. 이번에 minimal이 400이었던 것도 이 메시지로 바로 구분됐을 것입니다.
- **Gemini 3 온도 0.2**: Google은 Gemini 3 계열에서 temperature를 기본 1.0으로 두라고 권고하며, 낮추면 반복·품질 저하가 생길 수 있다고 명시합니다. 실호출 5회는 통과했으나 표본이 작으니, 남은 검증 항목에 "3.8에서 temperature 1.0 vs 0.2 비교"를 추가할 가치가 있습니다.
- **접두어 판정**: `gemini-3`/`gemini-2.5-flash` StartsWith는 lite 변형까지 포함하므로 의도대로 동작합니다. 다만 사용자 입력 모델명에 앞뒤 공백이 있으면 판정이 빗나가니 저장 시 Trim을 권장합니다.
- **메타데이터 파싱**: 성공 응답에서 `SimpleJson.Parse`가 실패하면 본문 추출 전에 예외가 납니다. 파싱 실패 시 메타데이터는 0으로 두고 텍스트 추출은 계속하도록 감싸면 안전합니다.
- **인스턴스 상태**: `LastModelVersion` 등은 인스턴스 필드라 동시 호출 시 섞일 수 있습니다. RequestGate가 직렬화한다면 문제없습니다.

### 남은 검증 제안

1. 3.8 이미지 경로 표본 확대(다른 형태·다른 범례 2~3종) 후 IoU 분산 확인.
2. 3.8의 응답 시간·토큰 비용을 2.5와 나란히 기록해 기본값 변경의 대가를 문서화.
3. 기존 사용자 저장값이 빈 문자열이거나 구 모델명일 때 Simple 기본이 3.8로 안전하게 승격되는지 로드 경로 확인.
4. 산→soil 오인은 범례로 해결됐으나, 설명 입력칸이 비어 있을 때의 기본 안내 문구가 범례 역할을 대신하는지 확인.

요약하면, 현재 클라이언트 코드는 배포를 막을 결함이 없고, 오류 본문 노출과 Gemini 3 온도 정책 두 가지만 후속으로 다루면 됩니다.