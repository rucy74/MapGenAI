# Text-first scope and generalization review

User now explicitly postpones image work (color mismatch remains unacceptable) and prioritizes text requests, including generalized terrain fills, location-constrained structures, and deleting existing features like rivers/deltas. Read-only consultation, actual Fable requested earlier and continuing.

Please inspect current dev source (baseline6b386b7): MapGen/WorldTileEditor.cs, TileMapState.cs, MapStateEditor.cs, MapParameterParser.cs, MapGenParams.cs, Patches/RiverPatches.cs, TerrainFromPatch.cs, and current prompt. No code modifications. Give concise grounded design plus current removal failure points and smallest safe implementation order.

Observed: river.present=false only sets hasRiver=false (also default); no removal hook in RiverPatches. WorldTileEditor operates mutators only, SurfaceTile.Rivers world links remain. RiverDelta is a River-category TileMutator. Unknown remove_mutators names currently aren't resolved (only additions are checked), so invented name can look successful. Prompt state includes authored shapes, but verify current actual/base features and availability list usefulness.

Proposed generalization: three backend families behind one typed, capability-aware request plan: (1) named region/geometry + terrain fill resolved through a controlled runtime TerrainDef catalog (lava among these), (2) spatial structure placement using region IDs and relations (inside/near/edge), with validated footprints and a supported generator catalog, (3) original world features with explicit keep/remove/restore rather than conflating unspecified with false. No universal 'any Def with arbitrary worker' success claims. Structure workers vary and some make quests/loot; preserve intended semantics with adapters. Capability limits should be explained, not claimed generated while substituting water for lava.

Please recommend clear semantics for remove RiverDelta vs remove all river: delta only could restore plain River when world river links exist, while full river removal should suppress all river-category generation for this selected map without corrupting neighboring world links. World metadata mutations must retain Undo/Reset and external edits. How to generalize targeted explicit category removal and validate unknown defs? Inspect installed game source via existing tools only if helpful; primary agent is decompiling actual worker/Tile methods locally. We will prioritize a verified deletion fix and a concrete extensible plan; don't assume all arbitrary structures can be implemented in one patch.
