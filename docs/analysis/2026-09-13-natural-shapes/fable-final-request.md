# Review actual implementation and evidence (read-only)

Please review the current diff since c1aef34. Focus production ContourWarp.cs, SdfComposite.cs, ShapeEdits/Validation, MapGenParams clone/Scribe, GenStepPatches, KO/EN Dialog_TextToMap/ShapeEditPrompt. Inspect NaturalShapeTests and native probe only where needed. Return concrete P0/P1 blockers, P2 limitations and whether the requested feature is supported. Do not edit files.

Your design feedback: schema includes composite+edge_roughness now; explicit named shapes go to composite, generic lakes to legacy bump. Non-composite edge_roughness rejected; none clears, null rejected explicitly. ID-less natural composites are cloned and assigned the same legacy IDs used in edit/current-state before rendering. No runtime string.GetHashCode/Rand.

Implementation uses shared shape-local 2D cubic value noise (same actual algorithm in tests and Mono). First native image looked too much like an oval: native/natural-shapes-generated-preview.png. Revised to three composed small warps with primary frequency4, secondary10 weighted .3, total max amplitude .09*primitive span. Each step displacement derivative norm bound <= 6*(.09/3)*(4+.3*10)/1.3 = .9693 (<1 for validated amount<=1). This bound uses each noise partial derivative<=3; not a generic Perlin/sine bound. Assess math and code independently. No promise for sub-cell gaps, nor rigid rotation of warped contours (rotation changes the base primitive, warp axes remain world-aligned).

Evidence: offline-tests-r2.log has70 PASS including 4 shapes*3 levels, narrow ring12seeds, deterministic render/translation/preset/clone/patch restore; provider/natural-results.json has7 real Gemini3.8 KO prompts intended+unrelated state preserved. Native-r2 in progress at request creation: real Scribe and real dialog undo plus full map generation and native Map Preview texture on a fixed soil base. First native result JSON was {} because SimpleJson serializes public fields rather than anonymous properties; test harness fixed with dictionaries and rerun. Do not count that first JSON as PASS. Final native-r2 result.json/observations/capture can be read if present. Production DLL currently47ed3355...; no changes yet installed.

User explicitly wants circles/stars/hearts precise by default and natural only on request. Lava terrain / placing ruins on an island remains deferred and not claimed implemented. Avoid expanding into unrelated settings/model/image issues.
